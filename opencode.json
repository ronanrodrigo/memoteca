{
  "$schema": "https://opencode.ai/config.json",
  "model": "opencode/mimo-v2.5-free",
  "instructions": [
    "AGENTS.md",
    ".memotek/agents/*.md",
    ".memotek/rules/*.md"
  ],
  "skills": {
    "paths": [
      ".memotek/skills"
    ]
  },
  "command": {
    "start": {
      "template": "Run make listen-issues to check for open issues with the memotek label. If you find pending issues, process each one following the pipeline: research → stack → implement → deploy → ci → pr → memory.",
      "description": "Check and process open issues with the memotek label"
    }
  }
}
