{
  "$schema": "https://opencode.ai/config.json",
  "model": "opencode/mimo-v2.5-free",
  "instructions": [
    "AGENTS.md",
    ".memoteca/agents/*.md",
    ".memoteca/rules/*.md"
  ],
  "skills": {
    "paths": [
      ".memoteca/skills"
    ]
  },
  "command": {
    "start": {
      "template": "Run make tasks-listen to query the central \"Memoteca\" GitHub Project board for items with Status=Todo (oldest first). If any, pick the oldest and follow the Assistant Work Loop: research → stack → implement → deploy → ci → pr → memory. Use make project-add-issue when filing new intake issues so they appear on the board.",
      "description": "Query the Memoteca board for Todo items and process the oldest"
    }
  }
}